/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {},
  // Allow larger body sizes for image/audio uploads (up to 10 MB)
  serverExternalPackages: [],
};

module.exports = nextConfig;
